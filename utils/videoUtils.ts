export const trimVideo = (videoSrc: string, start: number, end: number) => {
  // implementasi trimming video nanti (backend / MediaRecorder)
  console.log(`Trim video ${videoSrc} from ${start} to ${end}`);
};
