import React from 'react';

const Navbar = () => (
  <nav style={{ padding: '16px', textAlign: 'center', fontWeight: 'bold', letterSpacing: '2px' }}>
    DSRT — Digital Smart Revise Technology
  </nav>
);

export default Navbar;
