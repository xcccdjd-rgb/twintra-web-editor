import React from 'react';
// Simulasi fitur editor foto
const PhotoEditor = () => (
  <section>
    <h3>Photo Editor</h3>
    <p>[Crop, rotate, zoom, filter, export — powered by canvas]</p>
  </section>
);
export default PhotoEditor;
